<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.match.model.dao.MatchDao">
	<select id="calcPrice" resultType="grade">
		select grade_price, pass_count from grade 
			where match_count = #{matchCount} and #{memberGender} and #{memberGrade} and member_age like substr(#{memberAge}, 1,1} 
	</select>
</mapper>
